module SyntaxInFunctions    (
      triPattern
    , triGuard
    , triCase
    , qadd
    , qequal
    , qlist
    ) where

    -- prob.4-1
    triPattern :: Int -> Int
    triPattern = undefined

    triGuard :: Int -> Int
    triGuard = undefined

    triCase :: Int -> Int
    triCase = undefined

    -- prob.4-2
    qadd :: (Int, Int) -> (Int, Int) -> (Int, Int)
    qadd = undefined

    qequal :: (Int, Int) -> (Int, Int) -> Bool
    qequal = undefined

    qlist :: (Int, Int) -> [(Int, Int)]
    qlist = undefined
